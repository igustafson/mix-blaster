<%= form_for @mixtape do |f| %>
  <% if @mixtape.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@mixtape.errors.count, "error") %> prohibited
      this mixtape from being saved:</h2>
    <ul>
    <% @mixtape.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>